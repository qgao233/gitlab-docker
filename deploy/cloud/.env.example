# GitLab 云部署环境变量配置
# 复制此文件为 .env 并修改配置

# ============ GitLab 访问地址 ============
# 你的域名或公网 IP（不带 http://）
GITLAB_HOST=gitlab.example.com

# ============ PostgreSQL 配置（阿里云 RDS） ============
# 注意：密码包含特殊字符（如 # @ $）时需要用双引号包裹
DB_HOST=rm-xxxxx.pg.rds.aliyuncs.com
DB_PORT=5432
DB_NAME=gitlabhq_production
DB_USER=gitlab
DB_PASSWORD="your_db_password"

# ============ OSS 备份配置 ============
OSS_BUCKET=oss://your-bucket-name/gitlab-oss
