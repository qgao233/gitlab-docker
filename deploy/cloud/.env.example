# GitLab 本地部署环境变量配置
# 复制此文件为 .env 并修改配置

# ============ 部署模式 ============
# integrated: 集成模式（内置 PostgreSQL + 内置 Redis）
# external:   外部模式（外部 PostgreSQL + 内置 Redis）
DEPLOY_MODE=external

# ============ PostgreSQL 配置（外部模式使用） ============
# 集成模式下这些配置会被忽略
# 注意：密码包含特殊字符（如 # @ $）时需要用双引号包裹
DB_HOST=hq-mii-db.pg.rds.aliyuncs.com
DB_PORT=5432
DB_NAME=github-yfpo
DB_USER=github_user
DB_PASSWORD="your_db_password"


# ============ OSS 备份配置 ============
# Redis 使用 GitLab 内置（阿里云 Redis < 6.0 不兼容）
OSS_BUCKET=oss://your-bucket-name/gitlab-backups
